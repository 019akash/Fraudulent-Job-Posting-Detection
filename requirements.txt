Flask>=2.0
pandas>=1.3
numpy>=1.20
scikit-learn>=1.0  # Ensure compatible versions
joblib>=1.0
nltk>=3.6
category_encoders>=2.3 # Make sure this is installed
xgboost>=1.5 # Ensure compatible version used for training